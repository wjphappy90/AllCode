<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
        http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">

	<!-- 配置@Controller扫描 -->
	<context:component-scan base-package="cn.itcast.core.controller"/>

	<!-- 配合注解驱动 -->
	<mvc:annotation-driven conversion-service="conversionService"/>

	<!-- 配置视图解析器 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/portal/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>
	
	<!-- 配置自定义转换器 -->
	<bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
		<property name="converters">
			<set>
				<!-- 指定自定义转换器的全路径名称 -->
				<bean class="cn.itcast.core.common.CustomTrimStrConverter"/>
			</set>
		</property>
	</bean>
	
	<!-- 因为web.xml中对于DispatcherServlet的路径设置成了斜杠/, 所以会拦截所有, 但是不包括.jsp, 这样所有静态资源文件都
	无法访问, 可以在这里设置放行所有静态资源 -->
	<mvc:resources location="/css/" mapping="/css/**"/>
	<mvc:resources location="/js/" mapping="/js/**"/>
	<mvc:resources location="/images/" mapping="/images/**"/>
	
	<!-- 加载dubbo配置文件 -->
	<import resource="dubbo-portal.xml"/>
	
	<mvc:interceptors>
		<!-- 编写拦截器, 拦截去结算请求 -->
		<mvc:interceptor>
			<mvc:mapping path="/buyer/**"/>
			<bean class="cn.itcast.core.controller.interceptor.IsLoginInterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>
</beans>
